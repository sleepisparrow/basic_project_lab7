import 'package:flutter/material.dart';

class StuTFQuizWidget extends StatelessWidget {
  const StuTFQuizWidget({Key? key, required this.index}) : super(key: key);

  final int index;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
