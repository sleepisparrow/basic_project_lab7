import 'package:flutter/material.dart';

class DocumentPage extends StatelessWidget {
  const DocumentPage({Key? key}) : super(key: key);

  //네비게이션바의 질문눌렀을때 설문페이지
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('설문'),
    );
  }
}
